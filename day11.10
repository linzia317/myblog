注册GitHub并学习git基本操作
Git的基本操作包含几个主要步骤，用来管理和协作代码：

1. **初始化仓库**  
   在项目目录中使用 `git init` 命令来初始化一个新的Git仓库，这样就可以开始跟踪文件的更改。

2. **查看状态**  
   `git status` 命令可以显示当前仓库的状态，比如是否有未跟踪的文件，哪些文件已更改但未提交等。

3. **添加文件**  
   `git add 文件名` 可以将文件添加到暂存区，准备提交。如果想一次性添加所有改动，可以用 `git add .`。

4. **提交更改**  
   使用 `git commit -m "提交信息"` 提交更改，`-m` 后面跟的是对这次提交的描述，方便以后查阅。

5. **查看历史记录**  
   `git log` 可以查看提交的历史记录，包括每次提交的ID、作者和提交信息。

6. **创建分支**  
   使用 `git branch 分支名` 创建一个新的分支，可以并行开发，不影响主分支。

7. **切换分支**  
   用 `git checkout 分支名` 切换到指定分支上进行开发。

8. **合并分支**  
   `git merge 分支名` 可以把另一个分支的更改合并到当前分支上。

9. **克隆远程仓库**  
   使用 `git clone 仓库地址` 克隆一个远程仓库到本地，方便协作开发。

10. **推送到远程仓库**  
    `git push` 命令可以将本地的更改推送到远程仓库，通常会用到 `git push origin 分支名`。

11. **拉取更新**  
    使用 `git pull` 从远程仓库拉取最新的更改并合并到本地。
